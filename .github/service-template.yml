apiVersion: v1
kind: Service
metadata:
  name: {{ROUTE}}
  namespace: student
spec:
  type: ClusterIP
  ports:
    - containerPort: 5175
      name: rest
      protocol: TCP
  selector:
    app: {{ROUTE}}
    